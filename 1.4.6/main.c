#include <stdio.h>
#include <stdlib.h>
//Napisz program, który wczytuje liczbę calkowitą n (n > 2) i wypisuje iloczyn liczb
//parzystych z zakresu od 2 do n (czyli 2 * 4 * . . . * n dla n parzystych i 2 ∗ 4 ∗ . . . ∗ (n − 1) w przeciwnym wypadku)
int main()
{
    int i,n,odp;
    scanf("%d",&n);
    scanf("%d",&m);
    if(n<2)

        printf("N nie moze byc mniejsze od 2 ");

    if(n%2==1)
        n--;

    if(i=2;i<n;i+=2)
    {
        odp*=1;

    }
    printf("%d",odp);
    return 0;
}
